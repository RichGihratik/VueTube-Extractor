import type { ObjectToParse, ArrayToParse, PipelineItem, PipelineFn } from './types';
import { isObject, isArray } from './typeguards';

export const applyFlatten: PipelineFn = function (item: PipelineItem) {
  // TODO: Write flatten
  return item;
};
